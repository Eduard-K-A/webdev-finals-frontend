import React from 'react'

const User: React.FC = () => {
  return (
    <div>
      this is user page
      must list all admins, and Users
      must have a way to be able to create another admin account (role is initialized to admin)
    </div>
  )
}

export default User
